import { admin, authenticatedBusiness, cors, json } from "../_shared/subscription.ts";
Deno.serve(async(request)=>{if(request.method==="OPTIONS")return new Response("ok",{headers:cors});try{const{businessId}=await request.json();await authenticatedBusiness(request,businessId);const current=await admin.from("square_connection").select("access_token").eq("business_id",businessId).maybeSingle();if(current.data?.access_token){const environment=Deno.env.get("SQUARE_ENVIRONMENT")??"sandbox",base=environment==="production"?"https://connect.squareup.com":"https://connect.squareupsandbox.com";await fetch(`${base}/oauth2/revoke`,{method:"POST",headers:{Authorization:`Client ${Deno.env.get("SQUARE_APPLICATION_SECRET")??""}`,"Content-Type":"application/json","Square-Version":"2026-08-19"},body:JSON.stringify({access_token:current.data.access_token,client_id:Deno.env.get("SQUARE_APPLICATION_ID")})})}const removed=await admin.from("square_connection").delete().eq("business_id",businessId);if(removed.error)throw removed.error;return json({disconnected:true})}catch(error){return json({error:error instanceof Error?error.message:"Unable to disconnect Square."},400)}});

